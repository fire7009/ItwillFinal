<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.js.pjt.mapper.ProductMapper">

	<insert id="insertProduct" parameterType="com.js.pjt.vo.ProductVO">
		insert into product values(#{dlvr_no}, #{ord_no}, #{dlvr_empno}, sysdate,'N', sysdate)
	</insert>
	
	<select id="selectProduct" resultType="com.js.pjt.vo.ProductVO">
		select*from product where dlvr_no=#{dlvr_no}
	</select>
	
	<select id="selectListProduct" resultType="com.js.pjt.vo.ProductVO">
		select * from product order by prod_no
	</select>
   
   	<update id="updateProduct">
   		update product set ord_no=#{ord_no}, dlvr_empno=#{dlvr_empno}, dlvr_cmpl_dttm=sysdate where dlvr_no=#{dlvr_no}
   	</update>
   	
   	<delete id="deleteProduct">
   		delete from product where dlvr_no=#{dlvr_no}
   	</delete>
  
</mapper>